# Advanced Scenario

```
# Use a custom data directory
export PAIOS_DATA_DIR=/tmp/paios-data
paios add-note "Research voice control" --tags voice,ux
paios set-goal "Publish v0.2 roadmap" --deadline 2026-03-15 --kpi "Roadmap doc"
paios plan-week
paios knowledge-add "How to run demo?" "Use make demo and read examples/output"
paios knowledge-search "demo"
paios daily-reflect
```
